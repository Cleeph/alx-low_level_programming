#include "main.h"

/**
 * main - Print alphabet in lower case.
 *
 * Return: Always 0.
 */
int main(void)

{
char print_alphabet(void);
char n;
for (n = 'a'; n <= 'z'; n++)
_putchar(n);
_putchar('\n');
return (0);
}
