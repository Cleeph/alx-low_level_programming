#include "main.h"

/**
 * main - Print alphabet in lower case.
 *
 * Return: Always 0.
 */
void print_alphabet(void);

int main(void)
{
char *c = ("a..z");
char n;
for (n = 'a'; n <= 'z'; n++)
_putchar(n);
}

}
_putchar('\n');
return (0);
}
